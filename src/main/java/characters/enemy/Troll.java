package characters.enemy;

public class Troll extends Enemy {

    public Troll(String name, int maxHealth, int currentHealth, int attackPower, int healPower) {
        super(name, maxHealth, currentHealth, attackPower, healPower);
    }
}
